import React from 'react';

const TestiButton = props => (
  <div id="testi">
    <button type="button" onClick={props.fetchAllItems}>
      Testi Button
    </button>
  </div>
);

export default TestiButton;
